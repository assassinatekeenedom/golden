package action.command.html;

import action.command.HTML;
import action.Service;

public class Apache extends HTML {

    public Apache() {
        super(Service.APACHE);
    }

    @Override
    public String start() {
        return super.start();
    }
}
